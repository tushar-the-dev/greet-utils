const greet = require('./greet');
const farewell = require('./farewell');
const formatName = require('./utils/formatName');
const setGreet = require('./core/setGreet');

module.exports = {
  greet,
  farewell,
  formatName,
  setGreet
};